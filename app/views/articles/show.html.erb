<article class="mb-28">
  <%= render @article %>
  <div class="flex flex-col mt-9">
    <div class="flex w-full gap-2">
      <%= link_to "Edit 🖊", edit_article_path(@article), class: "flex items-center justify-center border-[1px] border-green-200 bg-green-300 hover:bg-green-400 hover:text-white hover:shadow-lg text-green-950 font-base text-xl px-2 pr-1 pl-5 rounded-full transition-all duration-100" if policy(@article).edit? %>
      <%= link_to "Back 👈", articles_path, class: "flex items-center justify-center border-[1px] border-gray-200 bg-gray-300 hover:bg-gray-400 hover:text-white hover:shadow-lg text-gray-950 font-base text-xl px-2 pr-1 pl-5 rounded-full transition-all duration-100" %>
    </div>
    <%= button_to "Destroy this article", @article, method: :delete, class: "mt-4 text-red-800 text-md underline decoration-2 underline-offset-2 font-bold" if policy(@article).destroy? %>
  </div>
</article>
